// Ads intrusivos de Adsterra eliminados (Popunder + Social Bar)
// Interceptaban clics del usuario y abrían ventanas con anuncios.
// Se mantienen solo los ads inline (AdBanner, AdNativeBanner).
export function AdScripts() {
  return null
}
